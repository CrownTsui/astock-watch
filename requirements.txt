# ===== 核心依赖（必需）=====
akshare>=1.13.0        # 免费行情/资金/新闻数据源（封装东方财富、巨潮等，无需 token）
pandas>=1.5.0          # 数据处理
numpy>=1.23.0          # 数值计算
scipy>=1.9.0           # 局部极值（支撑/压力、形态识别）
jinja2>=3.0.0          # HTML 报告模板渲染
requests>=2.28.0       # akshare 底层依赖 / 备用爬取
lxml>=4.9.0            # 网页解析（akshare 依赖）

# ===== 可选增强（缺失时自动降级，不影响运行）=====
snownlp>=0.12.3        # 中文情感分析；缺失则使用内置金融情感词库

# 说明：图表使用 Plotly.js（CDN）在浏览器端渲染，无需安装 plotly Python 包。
# 如需在 Python 端导出静态图片，可自行 `pip install plotly kaleido`。
